{
  "name": "coress-nft",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "hardhat compile",
    "clean": "hardhat clean",
    "deploy:sepolia": "hardhat deploy --network sepolia",
    "deploy:holesky": "hardhat deploy --network holesky",
    "task:lock": "hardhat lock-and-cross --network sepolia --tokenid 1",
    "task:lock:holesky": "hardhat lock-and-cross --network holesky --tokenid 1",
    "verify": "hardhat verify --network sepolia",
    "test": "hardhat test"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@chainlink/contracts": "^1.4.0",
    "@chainlink/contracts-ccip": "^1.6.1",
    "@chainlink/env-enc": "^1.0.5",
    "@chainlink/local": "^0.2.5",
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "@openzeppelin/contracts": "^5.4.0",
    "dotenv": "^17.2.2",
    "hardhat-deploy": "^0.12.4",
    "hardhat-deploy-ethers": "^0.4.2"
  },
  "dependencies": {
    "hardhat": "^2.22.0"
  }
}
